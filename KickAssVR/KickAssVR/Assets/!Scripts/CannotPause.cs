using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CannotPause : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PauseMenu.isMenu = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
