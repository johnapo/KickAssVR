using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanPause : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PauseMenu.isMenu = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
